#ifndef COLLMAP_HPP
#define COLLMAP_HPP

#include <vector>

class Collmap{
public:
  Collmap();
  Collmap(int width, int height);



private:
  int _w;
  int _h;
  std::vector<std::vector<int>>(_colisio);
  //, std::vector<int> (int, int));


};







#endif
